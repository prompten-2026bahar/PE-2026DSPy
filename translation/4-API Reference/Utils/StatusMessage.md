# /translation/status_message.md

## dspy.streaming.StatusMessage

```python
dspy.streaming.StatusMessage(message: str)
```

*(Veri Sınıfı / dataclass)*

Durum akışı (status streaming) için bir durum mesajını sarmalayan veri sınıfı (dataclass).

---

## Öznitelikler (Attributes)

### `message`

```python
message: str # örnek-özniteliği (instance-attribute)
```

---

## Functions (Fonksiyonlar)

*(Bu sınıf için listelenmiş herhangi bir fonksiyon bulunmamaktadır.)*
